# API base URL (only needed if NOT using the Vite proxy)
# When running with `npm run dev`, the proxy in vite.config.ts handles this automatically.
# Only set this if deploying the frontend standalone (e.g. Vercel, Netlify).
VITE_API_URL=http://localhost:5001/api
